// Markdown
const markdown = require('./markdown.md');

// Images
const image = require('../../../images/simplicity.png');

const data = {
  title: 'SimpliCity',
  image,
  body: markdown.body,
};

export default data;
