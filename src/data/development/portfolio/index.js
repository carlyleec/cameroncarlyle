// Markdown
const markdown = require('./markdown.md');

// Images
const image = require('../../../images/hydrasquid-120.png');

const data = {
  title: 'PORTFOLIO',
  image,
  body: markdown.body,
};

export default data;
