// Markdown
const markdown = require('./markdown.md');

// Images
const image = require('../../../images/other-120.png');

const data = {
  title: 'OTHER',
  image,
  body: markdown.body,
};

export default data;
