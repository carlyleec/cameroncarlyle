// Markdown
const markdown = require('./markdown.md');

// Images
const image = require('../../../images/steepslope.png');

const data = {
  title: 'Steep Slope',
  image,
  body: markdown.body,
};

export default data;
