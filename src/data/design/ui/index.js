// Markdown
const markdown = require('./markdown.md');

// Images
const image = require('../../../images/ui-120.png');

const data = {
  title: 'UI',
  image,
  body: markdown.body,
};

export default data;
