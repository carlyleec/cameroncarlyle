// Markdown
const markdown = require('./markdown.md');

// Images
const image = require('../../../images/fullstack-120.png');

const data = {
  title: 'FULL-STACK',
  image,
  body: markdown.body,
};

export default data;
